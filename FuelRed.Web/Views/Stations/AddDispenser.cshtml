@model FuelRed.Web.Data.Entities.DispenserEntity

@{
    ViewData["Title"] = "Add Department";
}

<h2>Add</h2>

<h4>Dispenser</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddDispenser">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdStation" />

            <partial name="_Dispenser" />

            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
                <a asp-action="Details" asp-route-id="@Model.IdStation" class="btn btn-success">Back to List</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
